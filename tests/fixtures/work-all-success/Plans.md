# Plans.md

| Task | Description | Status |
|------|-------------|--------|
| 1 | Implement `add` correctly in `src/math.js` and add `subtract` | cc:TODO |
| 2 | Add `src/format.js` so that `formatResult(label, value)` returns `"label: value"` | cc:TODO |
