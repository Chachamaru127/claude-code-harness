# bin/ artifacts are generated by `make build` (or scripts/build-harness.sh).
# Do not commit built binaries. Build locally before use.
#
# To build:
#   make build
# or:
#   cd go && go build -o ../bin/harness ./cmd/harness
harness
harness-darwin-amd64
harness-darwin-arm64
harness-darwin-arm64.exe
harness-linux-amd64
harness-windows-amd64.exe
harness.exe
harness-native
