package main

import (
	"fmt"
	"os"
)

// runCodexLoop is archived and not supported in the Claude-only v1 fork.
func runCodexLoop(_ []string) {
	fmt.Fprintln(os.Stderr, "codex-loop is archived/out of scope for Company AI Harness v1")
	os.Exit(1)
}
