{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://claude-code-harness.com/schemas/direction.schema.json",
  "title": "Direction Schema",
  "description": "演出システムのスキーマ。シーンごとの演出パラメータ（トランジション、強調、背景、タイミング）を定義します。",
  "version": "1.0.0",
  "type": "object",
  "required": ["scene_id", "transition", "emphasis", "background", "timing"],
  "properties": {
    "scene_id": {
      "type": "string",
      "description": "対象シーンのID（シナリオ内のシーンと一致する必要がある）",
      "pattern": "^[a-zA-Z0-9_-]+$"
    },
    "transition": {
      "type": "object",
      "description": "このシーンへのトランジション設定",
      "required": ["type", "duration_frames"],
      "properties": {
        "type": {
          "type": "string",
          "description": "トランジションの種類",
          "enum": ["fade", "slideIn", "zoom", "cut"]
        },
        "duration_frames": {
          "type": "integer",
          "description": "トランジションの長さ（フレーム数）",
          "minimum": 0,
          "maximum": 60,
          "default": 15
        },
        "easing": {
          "type": "string",
          "description": "イージング関数",
          "enum": ["linear", "easeIn", "easeOut", "easeInOut"],
          "default": "easeInOut"
        },
        "direction": {
          "type": "string",
          "description": "スライドの方向（slideInの場合のみ）",
          "enum": ["left", "right", "top", "bottom"],
          "default": "right"
        }
      },
      "additionalProperties": false
    },
    "emphasis": {
      "type": "object",
      "description": "シーン内での強調表現",
      "required": ["level"],
      "properties": {
        "level": {
          "type": "string",
          "description": "強調レベル",
          "enum": ["high", "medium", "low"]
        },
        "text": {
          "type": "array",
          "description": "強調するテキスト要素（キーワード・フレーズ）",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "sound": {
          "type": "string",
          "description": "効果音の種類",
          "enum": ["none", "pop", "whoosh", "chime", "ding"],
          "default": "none"
        },
        "color": {
          "type": "string",
          "description": "強調カラー（HEX形式）",
          "pattern": "^#[0-9A-Fa-f]{6}$",
          "default": "#00F5FF"
        },
        "position": {
          "type": "string",
          "description": "強調要素の配置",
          "enum": ["center", "top", "bottom", "left", "right"],
          "default": "center"
        }
      },
      "additionalProperties": false
    },
    "background": {
      "type": "object",
      "description": "背景設定",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "description": "背景の種類",
          "enum": ["cyberpunk", "corporate", "minimal", "gradient", "particles"]
        },
        "primaryColor": {
          "type": "string",
          "description": "プライマリカラー（HEX形式）",
          "pattern": "^#[0-9A-Fa-f]{6}$"
        },
        "secondaryColor": {
          "type": "string",
          "description": "セカンダリカラー（HEX形式）",
          "pattern": "^#[0-9A-Fa-f]{6}$"
        },
        "opacity": {
          "type": "number",
          "description": "背景の不透明度",
          "minimum": 0,
          "maximum": 1,
          "default": 1
        },
        "blur": {
          "type": "boolean",
          "description": "背景にブラーを適用するか",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "timing": {
      "type": "object",
      "description": "シーン内のタイミング調整",
      "properties": {
        "delay_before": {
          "type": "integer",
          "description": "シーン開始前の待機時間（フレーム数）",
          "minimum": 0,
          "default": 0
        },
        "delay_after": {
          "type": "integer",
          "description": "シーン終了後の待機時間（フレーム数）",
          "minimum": 0,
          "default": 0
        },
        "audio_start_offset": {
          "type": "integer",
          "description": "音声開始オフセット（フレーム数、正=遅延、負=早める）",
          "default": 30
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
