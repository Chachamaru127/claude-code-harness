---
_harness_template: "rules/security-guidelines.md.template"
_harness_version: "2.5.30"
description: "Security guidelines"
---

# Security Guidelines

> This rule was auto-generated by Claude harness.
> Purpose: Detect changes that require security attention and present a checklist.

## Patterns Requiring Security Attention

### File path detection

| Path Pattern | Attention Level | Reason |
|-------------|----------------|--------|
| `src/auth/`, `auth/` | 🔴 High | Authentication logic |
| `src/api/`, `api/`, `routes/` | 🟠 Medium | API endpoints |
| `middleware/` | 🟠 Medium | Request processing |
| `src/payment/`, `billing/` | 🔴 High | Payment processing |
| `config/`, `.env*` | 🟠 Medium | Sensitive configuration |

### Code change patterns

| Pattern | Attention Level | Reason |
|---------|----------------|--------|
| `password`, `secret`, `token` | 🔴 High | Authentication credentials |
| Dynamic code evaluation functions | 🔴 High | Arbitrary code execution risk |
| SQL query string concatenation | 🔴 High | SQL injection |
| Raw HTML insertion in React | 🟠 Medium | XSS risk |
| `cors()` configuration changes | 🟠 Medium | Cross-origin policy |

## OWASP Top 10 Compliance Checklist

### When changing authentication/authorization

- [ ] **A01: Broken Access Control**
  - Is authorization checked on all endpoints?
  - Is role-based access control correctly implemented?

- [ ] **A02: Cryptographic Failures**
  - Are passwords hashed? (bcrypt, argon2)
  - Is sensitive data encrypted?
  - Is HTTPS enforced?

- [ ] **A07: Identification and Authentication Failures**
  - Brute-force protection (rate limiting) in place?
  - Is session management appropriate?
  - Was MFA considered?

### When changing API endpoints

- [ ] **A03: Injection**
  - Is input sanitized?
  - Are parameterized queries used?
  - Is command injection prevented?

- [ ] **A04: Insecure Design**
  - Principle of least privilege applied?
  - Is input validation implemented at the business logic level?

- [ ] **A05: Security Misconfiguration**
  - Are unnecessary endpoints unexposed?
  - Are error messages not overly detailed?
  - Have default credentials been changed?

### When changing dependencies

- [ ] **A06: Vulnerable and Outdated Components**
  - Do added packages have known vulnerabilities? (`npm audit`)
  - Is the version up to date?
  - Is it actively maintained?

## Detection Logic for Skills

```
File change detected
    ↓
Path pattern match
    ↓
├── auth/, api/, payment/ → Display security checklist
├── middleware/ → CORS and authorization check
├── .env*, config/ → Sensitive data exposure check
└── Other → Normal processing
```

## Suggestion Templates

**When changing authentication-related code**:
> ⚠️ Authentication-related code change detected.
> Please review the security checklist:
> - [ ] Password hashing
> - [ ] Session management
> - [ ] Rate limiting

**When changing API endpoints**:
> ⚠️ API endpoint change detected.
> - [ ] Input validation
> - [ ] Authorization check
> - [ ] Error handling

## For VibeCoder (Plain Language)

| Technical Term | Plain Language |
|---------------|---------------|
| Authentication | Login / password-related |
| Authorization | Controlling who can do what |
| Sanitize | Neutralizing dangerous characters |
| Injection | Inserting malicious code |

**VibeCoder suggestion**:
> 🔐 This change involves login functionality.
> It is recommended to have a security expert review it.
> At a minimum, please verify:
> - Passwords are not stored in plain text
> - Login attempt limits are in place

## Notes

- **Warnings are suggestions**: the user makes the final call
- **False positives are acceptable**: err on the safe side
- **Expert review recommended**: encourage human review for critical changes
